import React from 'react';
import styles from './Login.module.css';

export default function LoginPage() {
    return (
        <div className={ styles.loginContainer }>
            <div class={ styles.layout_grid}>
                <div className={ styles.login_card }>
                    <h1>hah</h1>
                </div>
                <div className={ styles.login_card }>
                    <h1>wow</h1>
                </div>
            </div>
        </div>
    )
}